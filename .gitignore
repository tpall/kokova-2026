# GitHub renders the markdown reports itself — no need for pandoc output.
*.html

.Rproj.user/
.Rhistory
.RData
.Ruserdata
.DS_Store

# Local OSM extract for R/surface.R — ~120 MB, re-downloadable.
cache/
